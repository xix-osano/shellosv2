#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/shellos/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$SHELLOS_PATH/install/first-run/battery-monitor.sh"
  bash "$SHELLOS_PATH/install/first-run/firewall.sh"
  bash "$SHELLOS_PATH/install/first-run/dns-resolver.sh"
  bash "$SHELLOS_PATH/install/first-run/gnome-theme.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$SHELLOS_PATH/install/first-run/welcome.sh"
fi
